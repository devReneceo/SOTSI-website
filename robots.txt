# robots.txt — Seat of the Soul Institute (static prototype / Webflow migration target)
# Honored at the site root only (https://seatofthesoul.com/); the GitHub-Pages
# subpath preview does not serve this file at its host root.

User-agent: *
Allow: /

# Internal / non-public artifacts — never crawl.
Disallow: /reports/
Disallow: /report-9f3a2c/
Disallow: /prototype/
Disallow: /webflow-template/

# Note: the client-rendered shells (search/, blog/post/, podcast/episode/) and the
# About duplicate/hub carry <meta robots=noindex> and are intentionally left crawlable
# so those directives are read.

Sitemap: https://seatofthesoul.com/sitemap.xml
